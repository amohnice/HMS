@model HMS.Models.Restaurant.RestaurantTable
@{
    ViewData["Title"] = "Delete Table";
}

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="d-flex align-items-center mb-3">
            <a asp-action="Index" class="btn btn-sm btn-secondary me-3">
                <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
            </a>
            <h1 class="h3 mb-0 fw-bold">Delete Table</h1>
        </div>

        <div class="card border-danger shadow-sm">
            <div class="card-body p-4">
                <div class="text-center mb-4">
                    <svg class="text-danger mb-2" width="48" height="48" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path></svg>
                    <h4 class="fw-bold">Are you sure you want to delete Table @Model.TableNumber?</h4>
                    <p class="text-muted">This action cannot be undone. All database records associated with this table will be deleted or restricted.</p>
                </div>

                <div class="bg-light p-3 rounded mb-4">
                    <div class="row">
                        <div class="col-4 text-muted fw-bold">Table Number:</div>
                        <div class="col-8 fw-semibold">@Model.TableNumber</div>
                    </div>
                    <div class="row mt-2">
                        <div class="col-4 text-muted fw-bold">Capacity:</div>
                        <div class="col-8">@Model.Capacity Seats</div>
                    </div>
                    <div class="row mt-2">
                        <div class="col-4 text-muted fw-bold">Current Status:</div>
                        <div class="col-8">@Model.Status</div>
                    </div>
                </div>

                <form asp-action="Delete" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" asp-for="Id" />
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-danger btn-lg">Delete Table</button>
                        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
